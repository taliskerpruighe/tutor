# TODO — tutor

<!-- generated by `git-ops commit` — do not edit; edit the leaf TODO.md files -->

Open work by directory. The counts below are an index only — the
substance of each item lives in that directory's own `TODO.md`, which is
where entries are written and closed.

- *(nothing open)*
